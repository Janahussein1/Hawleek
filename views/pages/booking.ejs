<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Restaurant Reservations | Hawleek</title>
  <link rel="stylesheet" href="/css/style.css">
  <style>
    body { background:#f7fafc; color:#111827; font-family:Inter,system-ui,sans-serif; margin:0; }
    .page-shell { max-width:1120px; margin:0 auto; padding:24px; }
    .page-header { display:flex; align-items:flex-start; justify-content:space-between; flex-wrap:wrap; gap:16px; }
    .page-header h1 { margin:0; font-size:2.4rem; }
    .page-intro { max-width:720px; line-height:1.75; color:#4b5563; }
    .nav-links { display:flex; flex-wrap:wrap; gap:10px; margin:0; padding:0; list-style:none; }
    .nav-links a { color:#0f6e56; text-decoration:none; font-weight:600; padding:8px 12px; border-radius:10px; background:#f0fdf4; }
    .restaurant-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(320px,1fr)); gap:18px; margin-top:28px; }
    .restaurant-card { background:#fff; border:1px solid #e5e7eb; border-radius:18px; overflow:hidden; display:flex; flex-direction:column; box-shadow:0 18px 50px rgba(15,23,42,0.05); }
    .restaurant-card img { width:100%; aspect-ratio:16/10; object-fit:cover; }
    .restaurant-card-body { padding:22px; display:flex; flex-direction:column; gap:16px; }
    .restaurant-card-body h3 { margin:0; font-size:1.25rem; color:#111827; }
    .restaurant-card-body p { margin:0; color:#6b7280; line-height:1.7; }
    .restaurant-meta { display:grid; gap:8px; color:#4b5563; font-size:.95rem; }
    .reserve-btn { align-self:flex-start; border:none; border-radius:999px; background:#0f6e56; color:#fff; padding:12px 20px; cursor:pointer; font-weight:700; transition:transform .2s ease,background .2s ease; }
    .reserve-btn:hover { transform:translateY(-1px); background:#115f4f; }
    #reservation-panel { margin-top:28px; }
    .reservation-card { background:#fff; border:1px solid #e5e7eb; border-radius:22px; padding:24px; position:relative; box-shadow:0 24px 65px rgba(15,23,42,0.08); }
    .reservation-header { display:flex; align-items:flex-start; justify-content:space-between; gap:16px; margin-bottom:20px; }
    .reservation-header h2 { margin:0; font-size:1.75rem; }
    .eyebrow { margin:0 0 6px; color:#10b981; font-size:.9rem; font-weight:700; text-transform:uppercase; letter-spacing:.08em; }
    .subtitle { margin:0; color:#6b7280; line-height:1.8; }
    .close-button { border:none; background:transparent; font-size:1.4rem; color:#6b7280; cursor:pointer; }
    .reservation-card label { display:block; margin-bottom:16px; color:#374151; font-weight:600; font-size:.95rem; }
    .reservation-card input,
    .reservation-card textarea { width:100%; border:1px solid #d1d5db; border-radius:12px; padding:12px 14px; font-size:.95rem; color:#111827; outline:none; }
    .reservation-card textarea { resize:vertical; min-height:104px; }
    .form-grid { display:grid; grid-template-columns:repeat(auto-fit,minmax(220px,1fr)); gap:16px; }
    .form-actions { display:flex; flex-wrap:wrap; gap:12px; margin-top:10px; }
    .primary-btn, .secondary-btn { border:none; border-radius:12px; padding:12px 20px; cursor:pointer; font-weight:700; }
    .primary-btn { background:#0f6e56; color:#fff; }
    .secondary-btn { background:#f3f4f6; color:#374151; }
    .help-text { margin-top:16px; color:#6b7280; font-size:.95rem; }
    .loading-message, .notice, .error-message { margin:18px 0; color:#6b7280; }
    @media (max-width:720px) { .page-header { flex-direction:column; } }
  </style>
</head>
<body>
  <div class="page-shell">
    <header class="page-header">
      <div>
        <p class="eyebrow">Hawleek Restaurant Reservations</p>
        <h1>Reserve your table one restaurant at a time</h1>
        <p class="page-intro">Choose a restaurant, submit a separate reservation for each location, and receive a confirmation email with the restaurant name and time.</p>
      </div>
      <nav>
        <ul class="nav-links">
          <li><a href="/services">Local Directory</a></li>
          <li><a href="/clinics">Clinics</a></li>
          <li><a href="/transport">Transport</a></li>
          <li><a href="/mosques">Mosques</a></li>
          <li><a href="/toilets">Restrooms</a></li>
        </ul>
      </nav>
    </header>

    <div id="restaurant-list" class="restaurant-grid"></div>
    <div id="reservation-panel"></div>
  </div>

  <script src="/public/api.js"></script>
  <script src="/js/restaurantBooking.js"></script>
</body>
</html>
